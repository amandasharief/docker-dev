{
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=8.0.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^9",
        "phpstan/phpstan": "^1.6"
    },

    "scripts" : {
        "test": [
            "vendor/bin/phpunit"
        ],
        "coverage": [
            "vendor/bin/phpunit --coverage-html coverage"
        ]
    },
    "config": {
        "preferred-install": "dist"
      }
}
